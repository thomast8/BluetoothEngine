# SwiftPM
.build/
.swiftpm/

# Xcode
*.xcodeproj
*.xcworkspace
DerivedData/

# Build outputs (make-debug-app.sh outputs to dist/; legacy root bundle also excluded)
/dist/
/SensorDebug.app/

# macOS
.DS_Store

# Local assistant/runtime state
.claude/

# Local secrets — never committed
.env
*.local

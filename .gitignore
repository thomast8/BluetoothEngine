# SwiftPM
.build/
.swiftpm/

# Xcode
*.xcodeproj
*.xcworkspace
DerivedData/

# Built debug app bundle (make-debug-app.sh drops it at the repo root)
/SensorDebug.app/

# macOS
.DS_Store

# Local assistant/runtime state
.claude/

# Local secrets — never committed
.env
*.local
